1. Porque un lenguaje de programacion compila a lenguaje ensamblador, el cual es ejecutado por el sistema operativo. 
   El lenguaje ensamblador es un lenguaje de programacion de bajo nivel utilizado para manipular las instrucciones 
   internas de un dispositivo. Cada linea de codigo escrito del lenguaje ensamblador equivale perfectamente a una 
   instruccion del dispositivo que se esta programando (esto permite que no deba interpretarse ni compilarse para que el hardware "entienda"). 
   Depende de 2 cosas: de la arquitectura del procesador y la interpretacion que le de el sistema operativo. 
   Si en la computadora cambia la arquitectura del procesador, el lenguaje ensamblador es totalmente diferente, y no va a correr. 
   Ya que para cada arquitectura de procesador es necesario un lenguaje ensamblador especifico. Generalmente esto es siempre definido por el fabricante de hardware.

2. Oracle MV VirtualBox es una maquina virtual de sistemas, ya que puede emular una computadora completa.

3. Un hipervisor, o monitor de maquinas virtuales, es un proceso que crea y ejecuta maquinas virtuales. 
   Un hipervisor permite que un ordenador host preste soporte a varias maquinas virtuales invitadas mediante el uso compartido virtual de sus recursos, 
   como la memoria y el procesamiento.
4. No, ya que estan encapsuladas. No las afecta porque corren dentro del administrador de VMs y estan separadas unas de otras. 
   En caso de romperse el administrador, podria afectar a los demas elementos.